import{SUPPLEMENTS}from'./course-supplements.js';
const css=document.createElement('link');css.rel='stylesheet';css.href='course-advanced.css';document.head.append(css);
const slug=document.body.dataset.course,extra=SUPPLEMENTS[slug],host=document.querySelector('#course-lessons');
function render(){if(!extra||!host||host.querySelector('.advanced-section')||!host.querySelector('.module-block'))return false;const section=document.createElement('section');section.className='advanced-section';section.innerHTML=`<span class="tag">ADVANCED APPLICATION</span><h3>${extra.caseTitle}</h3><p>${extra.caseText}</p><h4>Practical lab</h4><ol>${extra.lab.map(x=>`<li>${x}</li>`).join('')}</ol><h4>Calculation and decision framework</h4><div class="formula-grid">${extra.formulas.map(x=>`<div>${x}</div>`).join('')}</div><div class="advanced-grid"><div><h4>Common failure patterns</h4><ul>${extra.mistakes.map(x=>`<li>${x}</li>`).join('')}</ul></div><div><h4>Professional checklist</h4><ul>${extra.checklist.map(x=>`<li>${x}</li>`).join('')}</ul></div></div><div class="ethics-note"><h4>Ethics and governance</h4><p>${extra.ethics}</p></div><h4>Further study</h4><ul>${extra.reading.map(x=>`<li>${x}</li>`).join('')}</ul><button class="button secondary" id="download-advanced-workbook">Download advanced workbook</button>`;host.append(section);section.querySelector('#download-advanced-workbook').onclick=()=>{const text=`ADVANCED COURSE WORKBOOK\n\n${extra.caseTitle}\n${extra.caseText}\n\nPRACTICAL LAB\n${extra.lab.map((x,i)=>`${i+1}. ${x}`).join('\n')}\n\nCALCULATION AND DECISION FRAMEWORK\n${extra.formulas.map(x=>'- '+x).join('\n')}\n\nCOMMON FAILURE PATTERNS\n${extra.mistakes.map(x=>'- '+x).join('\n')}\n\nPROFESSIONAL CHECKLIST\n${extra.checklist.map(x=>'- '+x).join('\n')}\n\nETHICS AND GOVERNANCE\n${extra.ethics}\n\nFURTHER STUDY\n${extra.reading.map(x=>'- '+x).join('\n')}`,url=URL.createObjectURL(new Blob([text],{type:'text/plain'})),a=document.createElement('a');a.href=url;a.download=`${slug}-advanced-workbook.txt`;a.click();URL.revokeObjectURL(url)};return true}
if(!render()&&host){const observer=new MutationObserver(()=>{if(render())observer.disconnect()});observer.observe(host,{childList:true,subtree:true});setTimeout(()=>observer.disconnect(),15000)}
