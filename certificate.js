import{requireCustomer,supabase,q}from'./portal-common.js';import{COURSES}from'./course-data.js';const s=await requireCustomer(),slug=new URLSearchParams(location.search).get('course');if(s&&COURSES[slug]){const {data,error}=await supabase.from('course_certificates').select('*').eq('course_slug',slug).single();if(error)location.replace(`course-${slug}.html`);else{q('#certificate-name').textContent=s.profile?.display_name||s.user.email;q('#certificate-course').textContent=COURSES[slug].title;q('#certificate-date').textContent=`Issued ${new Date(data.issued_at).toLocaleDateString()} · Version ${data.certificate_version||1}`;const url=new URL('verify-certificate.html',location.href);url.searchParams.set('code',data.verification_code);q('#certificate-code').innerHTML=`Status: <b>${data.revoked_at?'REVOKED':'VALID'}</b><br>Verification code: ${data.verification_code}<br><a href="${url}">${url}</a>`}}
