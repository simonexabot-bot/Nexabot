import{supabase}from'./portal-common.js';const params=new URLSearchParams(location.search),course=params.get('course'),position=Number(params.get('lesson')||0);async function enhance(){const host=document.querySelector('#lesson-copy');if(!host)return;const{data}=await supabase.from('course_modules').select('id,title,summary,chapter_body,estimated_minutes').eq('course_slug',course).eq('position',position).eq('status','published').maybeSingle();if(data){document.querySelector('#lesson-title').textContent=data.title;document.querySelector('#lesson-meta').textContent=`MODULE ${position+1} · ESTIMATED ${data.estimated_minutes} MINUTES · DATABASE CONTENT`;host.innerHTML=`<p>${data.summary}</p><div class="lesson-copy">${data.chapter_body.split('\n').map(x=>x.trim()?`<p>${x}</p>`:'').join('')}</div>`}const toolbar=document.querySelector('.toolbar');if(toolbar&&!toolbar.querySelector('[data-assessment]')){const a=document.createElement('a');a.className='button secondary';a.dataset.assessment='';a.href=`assessment.html?course=${course}&module=${position}`;a.textContent='Module assessment';toolbar.append(a)}supabase.rpc('customer_record_academy_event',{p_event:'lesson_view',p_course:course,p_properties:{module:position}})}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',enhance);else setTimeout(enhance,300);
