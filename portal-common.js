import {createClient} from 'https://esm.sh/@supabase/supabase-js@2';
export const config=window.NEXA_SUPABASE||{},supabase=createClient(config.url,config.publishableKey),q=s=>document.querySelector(s);
export async function requireCustomer(){const {data:{user}}=await supabase.auth.getUser();if(!user){location.replace('login.html');return null}const {data:profile}=await supabase.from('profiles').select('*').eq('id',user.id).single();if(['operator','compliance_admin'].includes(profile?.role)){location.replace('admin.html');return null}document.querySelectorAll('[data-customer-name]').forEach(x=>x.textContent=profile?.display_name||user.email);document.querySelectorAll('[data-logout]').forEach(x=>x.onclick=async()=>{await supabase.auth.signOut();location.replace('login.html')});if(location.pathname.endsWith('/portal.html')&&!document.querySelector('#anxb-commerce-links')){const section=document.createElement('section');section.id='anxb-commerce-links';section.className='content-card';section.style.marginBottom='50px';section.innerHTML='<h2>ANXB products and records</h2><p>Use approved airdrops and gifts on NEXA products, review your statement, or submit verified feedback.</p><div style="display:flex;gap:10px;flex-wrap:wrap"><a class="button" href="marketplace.html">ANXB marketplace</a><a class="button secondary" href="anxb-statement.html">Transaction statement</a><a class="button secondary" href="reviews.html">Submit a review</a></div>';document.querySelector('main')?.append(section)}return {user,profile}}
export const money=v=>new Intl.NumberFormat('en-US',{style:'currency',currency:'USD'}).format(Number(v||0));
