import{createClient}from'https://esm.sh/@supabase/supabase-js@2';const s=createClient(window.NEXA_SUPABASE.url,window.NEXA_SUPABASE.publishableKey),q=x=>document.querySelector(x);q('#verify-form').onsubmit=async e=>{e.preventDefault();const{data,error}=await s.rpc('verify_course_certificate',{p_code:q('#code').value.trim()}),valid=data?.valid;q('#verify-result').innerHTML=error?`<div class="notice">${error.message}</div>`:valid?`<div class="item"><h2>Valid certificate ✓</h2><p>Course: ${data.course_slug}</p><p>Issued: ${new Date(data.issued_at).toLocaleString()}</p><p>Version: ${data.certificate_version||1}</p></div>`:`<div class="notice">Certificate status: ${data?.status||'not found'}.</div>`};const code=new URLSearchParams(location.search).get('code');if(code){q('#code').value=code;q('#verify-form').requestSubmit()}
